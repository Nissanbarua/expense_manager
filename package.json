{
  "name": "expense-manager-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix server",
    "build": "npm install && cd server && npm run build && cd ../client && npm install && npm run build",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^25.6.0",
    "@types/express": "^5.0.6",
    "@types/mongoose": "^5.11.96",
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.19",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/morgan": "^1.9.10"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.6.1",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.1",
    "dotenv": "^17.4.2",
    "helmet": "^8.1.0"
  }
}
